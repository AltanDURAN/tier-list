{% block body %}
<h1>Welcome, {{ app.user.email }}</h1>

<a href="{{ path('profile') }}">
    <i class="fa fa-user"></i> Profile
</a>

<a href="{{ path('app_logout') }}">
    <button>Se déconnecter</button>
</a>

{% if is_granted('ROLE_ADMIN') %}
    <br><br>
    <a href="{{ path('user_create') }}"><button>Ajouter un utilisateur</button></a>
    <br><br>
    <a href="{{ path('admin_create') }}"><button>Ajouter un admin</button></a>
    <br><br>
    <a href="{{ path('user_list') }}"><button>Voir tous les utilisateurs</button></a>
{% endif %}


{% endblock %}
