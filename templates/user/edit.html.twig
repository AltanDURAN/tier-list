{% extends 'base.html.twig' %}

{% block title %}Éditer l'utilisateur{% endblock %}

{% block body %}
<h1>Éditer l'utilisateur {{ user.email }}</h1>

{{ form_start(form) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password) }}
    {{ form_row(form.save) }}
{{ form_end(form) }}

<a href="{{ path('user_list') }}"><button>Retour à la liste</button></a>

{% endblock %}
